############################
# Backend Configuration
############################

# Banco de dados. Para SQLite, use: sqlite:///./data.db
DATABASE_URL=postgresql+psycopg2://neondb_owner:npg_3SiPtu6UBOfL@ep-green-sky-acefqmsv-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Estratégia de multi‑tenancy: 'db', 'schema' ou 'column'
TENANCY_STRATEGY=column

# Segurança JWT
JWT_SECRET=changeme
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_MINUTES=1440

# Contato do controlador de dados (LGPD)
PRIVACY_CONTACT_EMAIL=privacidade@example.com

# SMTP (opcional)
SMTP_SERVER=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

# Outros
# Ajuste CORS se necessário (separados por vírgula)
CORS_ORIGINS=http://localhost:5173

# Número de workers Uvicorn (para deploy em produção)
UVICORN_WORKERS=4

# Pool SQLAlchemy (ajustado para limite do Neon free)
SQLALCHEMY_DISABLE_POOL=true
SQLALCHEMY_POOL_SIZE=2
SQLALCHEMY_MAX_OVERFLOW=2
SQLALCHEMY_POOL_TIMEOUT=30
SQLALCHEMY_POOL_RECYCLE=1800
