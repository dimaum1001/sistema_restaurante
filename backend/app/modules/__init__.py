# Module namespace for domain-specific building blocks.

"""Domain modules package.

Each submodule under ``app.modules`` encapsulates business rules,
specialized queries and transfer objects (schemas) that can be shared
across multiple API routers.
"""

# Deliberately empty; submodules expose their own public interfaces.
